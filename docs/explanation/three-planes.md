# Three independent planes

Brainstem through Prefrontal Cortex is a responsibility/authority topology, not an execution order. The dependency DAG states which
layer consumes which constraints or state. Compile order is an explicit topological ordering. Actor/Role
manifests are separate because capability and escalation do not redefine Brain responsibility.
