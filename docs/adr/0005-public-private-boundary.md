# ADR 0005: public specification, external private overlays

Status: accepted. The repository contains generic schemas and synthetic fixtures. Real identity, memory,
session, state, credentials, and operational evidence stay in an external private store. References may
name an environment variable but never contain its secret value.
