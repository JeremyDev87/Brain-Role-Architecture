# ADR 0004: local deterministic validator

Status: accepted. The reference CLI uses local files, stable errors, bounded parsing, and no network or
dynamic execution. Deterministic conformance is kept separate from future model-behavior evaluations.
