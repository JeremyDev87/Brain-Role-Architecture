# Compile order

Every instance declares Brainstem through Prefrontal Cortex exactly once. Brainstem compiles first. A layer compiles only after all declared
dependencies. An order can differ from numeric P order; the minimal example compiles Prefrontal Cortex before Default Mode Network.
