# Precedence

Host/system safety authority remains outside an instance. Within a conforming instance, P0 constraints
bind every P1-P6 asset and every role. No higher layer or role can override P0. Conflicts stop validation
rather than selecting a plausible winner.
